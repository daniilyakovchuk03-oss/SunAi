# ---- Режим ----
# mock  = ничего наружу не уходит, всё локально (для тестового чата)
# live  = реальные запросы в Wazzup / amoCRM / LLM
MODE=mock

# ---- Wazzup ----
WAZZUP_API_KEY=
WAZZUP_CHANNEL_ID=
# crmUserId, которым помечаются сообщения ИИ. Любая строка, но постоянная.
AI_CRM_USER_ID=ai-agent

# ---- amoCRM ----
AMO_SUBDOMAIN=
AMO_ACCESS_TOKEN=
AMO_CLIENT_ID=
AMO_CLIENT_SECRET=
AMO_REDIRECT_URI=

# ---- LLM ----
# stub = встроенная заглушка без сети и без расхода токенов
LLM_PROVIDER=stub
ANTHROPIC_API_KEY=
LLM_MODEL=claude-haiku-4-5-20251001

# ---- Служебное ----
DB_PATH=./data/agent.db
RULES_PATH=./rules.yaml
# Пауза перед ответом ИИ — чтобы менеджер успел перехватить
REPLY_DELAY_SECONDS=8
# Секрет для проверки входящих вебхуков (произвольная строка)
WEBHOOK_SECRET=change-me
TZ=Asia/Almaty
